import Flag from "../components/Flag";

const FlagPage = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
        width: "100vw",
      }}
    >
      <Flag />
    </div>
  );
};

export default FlagPage;
